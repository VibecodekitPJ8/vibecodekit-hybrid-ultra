name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: pytest + audit (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # eval_select needs the merge-base — fetch full history.
          fetch-depth: 0

      - name: Assert canonical org (drift guard, hard fail)
        # Hard fail.  KHÔNG có cơ chế bypass — fork CI phải tự sync ALLOWED_ORGS
        # hoặc fork với canonical org khớp.  Xem
        # ``tests/test_repo_urls_canonical.py``: canonical org FINAL là
        # VibecodekitPJ8 (cycle 8 PR1, sau 9 lần rebrand — ĐÂY LÀ LẦN CUỐI).
        # Anti-pattern env-gated bypass (cycle 5) đã được loại bỏ ở
        # PR1 cycle 6 — quy tắc tự tắt được không phải quy tắc.
        if: github.event_name == 'push' || github.event_name == 'pull_request'
        run: |
          owner="${{ github.repository_owner }}"
          echo "owner=$owner"
          if [ "$owner" != "VibecodekitPJ8" ]; then
            echo "::error::Canonical org là VibecodekitPJ8 (cycle 8 final lock).  Fork CI: sync ALLOWED_ORGS hoặc pytest -k 'not test_repo_urls_canonical'."
            exit 1
          fi
          # Cross-check git remote (chống case repo settings drift).
          remote="$(git config --get remote.origin.url || true)"
          echo "remote=$remote"
          if ! echo "$remote" | grep -qE "VibecodekitPJ8/"; then
            echo "::error::remote.origin.url không match VibecodekitPJ8/ — drift."
            exit 1
          fi

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install pytest + ruff + mypy + coverage + pyyaml (core stays stdlib-only)
        # Cycle 7 PR2: pyyaml là dev-only — `vn_error_translator` degrade
        # gracefully khi PyYAML absent, nhưng CI cần test coverage cho
        # YAML loader path nên cài luôn.
        run: |
          python -m pip install --upgrade pip
          pip install pytest ruff mypy coverage pyyaml

      - name: Lint (ruff F401/F841/F811)
        # PR2: zero-tolerance cho unused imports / unused locals / shadowed
        # imports.  Style rules KHÔNG bật ở stage này.
        env:
          PYTHONPATH: ./scripts
        run: |
          python -m ruff check scripts/vibecodekit --select F401,F841,F811

      - name: mypy strict (9 core modules)
        # Cycle 6 PR2: strict gate cho 5 module (permission_engine,
        #   scaffold_engine, verb_router, denial_store, _audit_log).
        # Cycle 7 PR3: mở rộng strict cho 4 module subprocess/runtime
        #   (tool_executor, team_mode, task_runtime, subagent_runtime)
        #   — block ``strict = False`` trong ``mypy.ini`` đã bị xoá.
        env:
          PYTHONPATH: ./scripts
        run: |
          python -m mypy --strict \
            scripts/vibecodekit/permission_engine.py \
            scripts/vibecodekit/scaffold_engine.py \
            scripts/vibecodekit/verb_router.py \
            scripts/vibecodekit/denial_store.py \
            scripts/vibecodekit/_audit_log.py \
            scripts/vibecodekit/tool_executor.py \
            scripts/vibecodekit/team_mode.py \
            scripts/vibecodekit/task_runtime.py \
            scripts/vibecodekit/subagent_runtime.py

      - name: eval_select preview (diff-based test selection)
        # Visibility-only on every event: prints the selection so reviewers
        # can see which tests would have been chosen.  We always run the
        # FULL suite below for safety, but expose the diff-based preview
        # so the eval_select pipeline is exercised and any breakage is
        # caught immediately on CI rather than at /vck-ship time.
        # ``continue-on-error`` so an eval_select bug never blocks the
        # mandatory quality gates (pytest / audit / release-matrix).
        continue-on-error: true
        env:
          PYTHONPATH: ./scripts
          BASE: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || 'origin/main^' }}
        run: |
          git fetch origin --no-tags --depth=200 "${BASE#origin/}" || true
          python -m vibecodekit.eval_select \
            --base "$BASE" \
            --map tests/touchfiles.json \
            --json | tee eval_select.preview.json
          # Emit a one-line summary for the workflow log.
          python -c "import json; d=json.load(open('eval_select.preview.json')); print(f\"selected={len(d.get('selected') or [])} unmapped={len(d.get('unmapped_changes') or [])}\")"

      - name: pytest (full suite)
        env:
          VIBECODE_UPDATE_PACKAGE: ${{ github.workspace }}/update-package
          PYTHONPATH: ./scripts
        run: |
          python -m pytest tests -q

      - name: conformance_audit (internal self-test)
        env:
          VIBECODE_UPDATE_PACKAGE: ${{ github.workspace }}/update-package
          PYTHONPATH: ./scripts
        run: |
          python -m vibecodekit.conformance_audit --threshold 1.0

      - name: pytest with coverage (Phase 2a — TOTAL >=70, tool_executor >=80)
        # Cycle 6 PR3 (Phase 1): per-module gate cho tool_executor.py >=80%
        #   + global TOTAL fail_under=60.
        # Cycle 7 PR2 (Phase 2a): vn_faker / vn_error_translator / team_mode
        #   đạt ≥80% mỗi module.  Global TOTAL fail_under=70 (xem
        #   pyproject.toml [tool.coverage.report]).  cli.py + deploy_orchestrator
        #   tạm trong omit cho tới Phase 3.
        env:
          VIBECODE_UPDATE_PACKAGE: ${{ github.workspace }}/update-package
          PYTHONPATH: ./scripts
        run: |
          python -m coverage run --source=scripts/vibecodekit -m pytest tests -q
          # Global TOTAL gate (fail_under=70 in pyproject.toml).
          python -m coverage report
          # Per-module gates (Phase 2a):
          python -m coverage report --include='scripts/vibecodekit/tool_executor.py' --fail-under=80
          python -m coverage report --include='scripts/vibecodekit/team_mode.py' --fail-under=80
          python -m coverage report --include='scripts/vibecodekit/vn_faker.py' --fail-under=80
          python -m coverage report --include='scripts/vibecodekit/vn_error_translator.py' --fail-under=80
          python -m coverage xml -o coverage.xml

      - name: Upload coverage XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml

      - name: validate release matrix (L1 + L2 + L3)
        env:
          PYTHONPATH: ./scripts
        run: |
          python tools/validate_release_matrix.py \
            --skill . \
            --update ./update-package
